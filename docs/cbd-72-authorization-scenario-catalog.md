# CBD-72 — Authorization Scenario Catalog

| Field | Value |
| --- | --- |
| Status | **Approved v0.1.14 — final package approved by the Product Owner, August 18, 2026; deterministic fixtures are test-design scope under VT-94-*. A proposed `MAN-03` scenario for permission-model row 36 (manual account, `OQ-236-011`) is added at v0.1.15 and not yet approved; the document remains controlling at v0.1.14 pending that approval under `PO-CONTRACT-APPROVALS-001`.** |
| Version | 0.1.14 |
| Owner | Alexander Wohlford |
| Jira | [CBD-72](https://cobudget.atlassian.net/browse/CBD-72) |
| Governing specification | `docs/cbd-72-collaboration-permission-model.md` |
| Traceability | `docs/cbd-72-acceptance-criteria-traceability.md` |
| Last updated | August 16, 2026 |

## 1. Purpose and assertion contract

This catalog supplies deterministic acceptance evidence for the CBD-72 permission model. Every completed scenario must state the actor and active membership, budget space, target resource and scope, initial versions, action, expected visible result, protected-state delta, audit outcome, and notification outcome.

A denied scenario passes only when the action is unavailable in the interface, a direct request is denied server-side, protected state is unchanged, no restricted data is returned or inferred, and a safe audit event is recorded.

## 2. Scenario families

| Prefix | Family | Minimum coverage |
| --- | --- | --- |
| ROLE | Role action matrix | One positive and one denied case for every nontrivial role/action boundary |
| VIEW | Viewer profiles and masking | No-profile default, hierarchical profiles/groups, scope changes, derived data, search, report, alert |
| PART | Accountability Partner | Comprehensive read, masking boundary, comments, acknowledgements, informational-alert safeguard |
| COLL | Collaborator mutations | Planning, reconciliation, categorization, manual transaction, notes, prohibited administration/export |
| OWN | Ownership and Co-owner safeguards | Counts, invitation, removal, transfer, reauthentication, stale/concurrent state |
| CONN | Bank connections | Authorizer-only management, other-owner denial, private configuration, authorizer loss |
| XSP | Cross-budget isolation | Different roles, foreign identifiers, query/cache/job/export/notification isolation |
| AUTH | Enforcement | Protected read, mutation recheck, stale work, default deny, concurrency |
| CAT | Category lifecycle | Archival with history, dependency blocking, restore, absence of hard delete |
| REC | Reconciliation mutations | Income confirm/reject, expected/actual match, pending/posted match, split, dismiss |
| DATE | Derived budget-date override | Reclassification, immutable source dates, recalculation, provenance audit |
| VIS | Visibility and derived data | Role-bounded reads, profile inheritance, masking, derived noninterference |
| EXP | Exports and snapshots | Financial export, administrative-history export, owner-authorized Viewer snapshot, categorical denials |
| REP | Report generation | Profile support, input completeness, cache binding, no conveyed export right |
| MEM | Membership administration | Invitation, revocation, removal, role transition, profile lifecycle |
| SET | Shared settings | Ordinary configuration, personal-account exclusion, connection exclusion |
| ALERT | Alert event, instance, and delivery | Event dedup/resolution, mandatory instances, personal state, delivery retry and privacy |
| MAN | Manual-transaction lifecycle | Dependency blocking, removal, 30-day restore, purge tombstone |
| INT | Notes and comments | Author-only mutation, cross-author moderation denial, target-bound visibility, revocation |
| AUD | Audit | Success, denial, safe delta, attribution, policy version, secret minimization |
| LIFE | Membership and budget lifecycle | Inactive membership, revocation, archival, deletion, orphan prevention, sole-owner exit, recovery boundary |

## 3. Initial scenario inventory

| ID | Scenario | Expected outcome | Governing rules | Draft status |
| --- | --- | --- | --- | --- |
| ROLE-01 | Each role attempts to create, edit, cancel, and confirm a schedule. | Primary Owner, Co-owner, and Collaborator succeed when current authorization and schedule-state predicates pass; Viewer and Accountability Partner are denied without state change. Every attempt produces the required audit evidence. | PM-72-002–005/007; matrix §4.1 permission 5 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| ROLE-02 | Collaborator creates and edits a budget plan, archives and restores one confirmed category, then attempts to discard a plan that became active. | Plan create/edit and category archive/restore succeed and are audited; the archived category remains in history but cannot receive new assignments. Active-plan discard is denied with no state change and a denial audit event. An owner may discard only a never-active draft after explicit confirmation. | PM-72-002–005/007; matrix §2.3 and §4.1 permissions 2–4 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| ROLE-03 | Collaborator creates and edits a bill and a goal, archives/restores them after confirmation, and discards an unused draft. | Confirmed resources archive/restore with retained history and no new future occurrences or targets. Only a never-confirmed, never-active, unreferenced draft is permanently discarded. Every mutation is attributed and audited. Viewer and Accountability Partner attempts are denied without state change. | PM-72-002–005/007; matrix §4.1 permission 3 and §5.5 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| ROLE-04 | Primary Owner, Co-owner, Collaborator, Viewer, and Accountability Partner each attempt the full membership-administration set: invite a non-owner, revoke a pending invitation, remove an active member, change a non-owner role, and assign a Viewer visibility profile. | Primary Owner and every Co-owner succeed within the owner safeguards; each transition is atomic, notified, and audited. Collaborator, Viewer, and Accountability Partner attempts offer no interface control, are denied server-side, change no membership or profile state, and produce denial audit events. No role reaches Co-owner appointment, Co-owner removal, or Primary transfer through this path. | Matrix §4.3 permissions 24–25; PM-72-001–004 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| VIEW-01 | New Viewer accepts membership with no visibility profile. | No financial or schedule resources, totals, searches, reports, or alerts are visible. Role membership alone confers no data scope. | §5.1 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| VIEW-02 | Owner assigns Account-group profile with the “Household accounts” group containing two accounts. | Safe identity, balances, all readable transactions, reconciliation history, interactions, and interpretation-only category labels for those accounts appear. Other accounts, whole-category targets, global totals, administration, and secrets remain absent. | PM-72-006; §5.1 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| VIEW-03 | Viewer profile is removed or its scope groups change while a report, cached view, alert, and safe export package exist. | The atomic change immediately invalidates open work and every derived access path; refresh, search, report reopen, alert open, and pending download use the new profile or are denied without leakage. Audit records before/after profile, groups, and later denials. | PM-72-002–006; matrix §4.2 permission 22 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| VIEW-04 | Viewer searches for a hidden transaction using its exact merchant, amount, description, and identifier and exercises suggestions and filters. | No result, count, suggestion, autocomplete entry, filter value, empty-state distinction, or other existence signal is returned. A matching cross-space record is equally excluded. | PM-72-006/010; matrix §4.2 permission 17; §5.2 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| VIEW-05 | Viewer opens profile-accessible content whose interaction identifies another member. | The fixed interpretation envelope includes only shared settings and member identity/role needed to understand that content; unrelated members and settings remain undisclosed. | Matrix §4.1 permission 1; §5.1 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| VIEW-06 | Owner attempts to combine Account-group and Category-group scopes on one Viewer membership. | The configuration is rejected atomically with no hybrid scope. Owner may select multiple groups only within one profile type or replace the profile type entirely. | Matrix §5.1; PM-72-001–004 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| VIEW-07 | A firm alert and an informational alert are both generated from an account that falls partly inside and partly outside an Account-group Viewer's current profile. | The Viewer receives one mandatory in-app instance for the firm alert only when the settled fact is entirely readable within the current profile, and its payload carries no hidden amount, balance, counterparty, or existence signal. The Viewer may acknowledge that personal instance. A firm fact depending on any unreadable input produces no Viewer instance rather than a redacted one. No informational-alert instance is ever created for a Viewer. Owner and Partner instances for the same shared event are unaffected. | §5.1 item 10; §5.4.1; matrix §4.1 permissions 12–13; PM-72-006 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| PART-01 | Active Partner opens every budget-space financial/schedule resource and attempts excluded fields plus a resource-specific grant change. | Comprehensive financially read-only scope appears under the fixed field boundary, including readable merchant/payee display names. Credentials, full account numbers, connection management, personal alert state, administration/security data, telemetry, and cross-space data remain hidden. Resource-level Partner grants are rejected; Viewer is required for partial sharing. | §5.3; matrix §§4.1–4.2 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| PART-02 | Partner comments and acknowledges their personal firm-alert instance while another eligible recipient leaves theirs unacknowledged. | Attributed interaction records are created; no financial or schedule state changes. The Partner's acknowledgement changes only their personal alert state; the other recipient's alert remains unacknowledged and visible. | Matrix §4.1 permission 12; §§5.3–5.4 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| PART-03 | Owner attempts to retain Partner while narrowing visibility, then changes the membership to Viewer and assigns Category-group profile. | Partial Partner configuration is rejected with no hybrid state. The role change and single Viewer profile succeed as distinct audited actions, and current access is recalculated immediately. | PM-72-001–004; matrix §4.2 permission 23; §§2.1/5.1/5.3 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| PART-04 | Primary Owner and Co-owner each attempt to view and then change an Accountability Partner's channel destinations and event/category delivery preferences, through the interface and by direct request. | No control is offered, and every direct request is denied: notification channels and delivery preferences are personal-account settings owned by the recipient. No preference, destination, alert eligibility, membership, or financial state changes. Denials are audited without recording the Partner's destinations, and the response reveals neither the destinations nor whether any are configured. The Partner's own settings remain readable and editable only by the Partner. | §5.4; §5.4.1 delivery layer; PM-72-002–004; CBD-69 `ALT-07` | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| PART-05 | Accountability Partner enables push and disables email for a supported event category in their personal account, then a firm event fires for which they are eligible. | The preference change succeeds and affects only that Partner's delivery attempts. Budget-space access, alert eligibility, the shared event, and other recipients' instances are unchanged. The mandatory in-app instance is still created regardless of channel preferences, and only the push attempt is made. No other member can view or infer the destinations, and the change is recorded in personal-account rather than budget-space history. | §5.4; §5.4.1 items 3 and 7; PM-72-002; CBD-69 `ALT-07` | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| PART-06 | Accountability Partner attempts a schedule mutation, a manual-transaction edit, and a Viewer-profile change, then acknowledges their own firm-alert instance and comments on a supported readable target. | Every financial, schedule, and administrative mutation is denied with no state change and a denial audit event, because the role is financially read-only. The personal firm-alert acknowledgement and the attributed comment both succeed, remain attributed to the Partner, and change no financial, schedule, permission, membership, connection, or configuration state. | Matrix §4.1 permissions 5, 9, 11a, 12; §§5.3/5.6; PM-72-002–004 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| COLL-01 | Collaborator categorizes an imported transaction. | Category changes and affected totals/alerts recalculate; bank-provided fields remain immutable; actor and before/after category are audited. Viewer and Accountability Partner attempts are denied without state change. | PM-72-002–005/007/009; matrix §4.1 permission 8 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| COLL-02 | Collaborator creates an unlinked manual transaction, adds a note, edits and removes the transaction, then restores it within 30 days. | Creation, note, and edit succeed with manual provenance and actor attribution. Removal immediately excludes the transaction from ordinary views, search, reports, exports, and calculations and atomically recalculates totals/alerts. Restore reverses those effects against current state. Manual provenance and lifecycle audit remain intact; the record never acquires bank-imported provenance. Viewer and Accountability Partner attempts are denied. | PM-72-002–005/007/009; matrix §4.1 permissions 9/11 and §5.5 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| COLL-03 | Each role submits a direct request to edit a bank-imported transaction's amount, merchant, account, source date, lifecycle state, or institution identifier. | Every request is denied without source or financial-state change and creates a safe denial audit event. Supported categorization, budget-date override, notes, reconciliation, and dispute workflows remain separate overlays. | PM-72-002–005/009; matrix §4.1 permission 10 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| OWN-01 | Primary Owner invites two eligible members as Co-owners and both recipients accept. | Invitations create no active owner authority before acceptance. Each acceptance independently rechecks recipient eligibility and membership state; both recipients become active Co-owners because multiple Co-owners are permitted. The sole Primary Owner and all connection-authorizer boundaries remain unchanged; relevant parties are safely notified and each transition is audited. | PM-72-001–005/008; matrix §4.3 permission 26 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| OWN-02 | Primary Owner removes a specific Co-owner after fresh reauthentication while that Co-owner has open work and personally authorized bank connections. | Current authority/version checks pass; removal commits atomically; the affected former Co-owner's access, open work, caches, alerts, reports, and pending downloads invalidate; other Co-owners remain unchanged; both parties are notified and outcome is audited. Connection authority does not transfer automatically. | PM-72-002–005/008; matrix §4.3 permission 27; §6.1 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| OWN-03 | Any role, including the Primary Owner, submits an ordinary membership request to remove, demote, deactivate, or inactivate the Primary Owner. | Control is absent and direct request is denied/audited without any membership, ownership, connection, or financial-state change. The budget space retains exactly one active Primary Owner. | PM-72-002–004/008; matrix §4.3 permission 28; §6.3 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| OWN-04 | Accepted ownership transfer commits while multiple Co-owners already exist; a separate attempt uses a stale recipient membership/role version. | The valid transfer atomically makes the recipient the sole Primary Owner, makes the outgoing Primary another Co-owner, and leaves every existing Co-owner unchanged. The stale attempt is blocked with no partial role, ownership, membership, or connection-authority change. Each outcome is safely notified and audited as one event group. | §6.2; PM-72-001–005/008; matrix §4.3 permission 29 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| OWN-05 | Primary Owner offers ownership transfer separately to an active Viewer and an active Accountability Partner; each authenticates and accepts the versioned Primary-access disclosure. | Either active role may transition directly to sole Primary without an intermediate role. Viewer profile/groups or Partner role-specific state closes atomically; required consent/audit history remains, personal preferences remain personal, stale artifacts and alert eligibility are recalculated, the outgoing Primary becomes a Co-owner, existing Co-owners remain unchanged, and connection authority does not transfer. | PM-72-001–005/008–009; matrix §4.3 permission 29; §6.2 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| OWN-06 | Primary Owner attempts transfer to a pending invitee, expired/revoked/inactive member, or the Primary themself; alternatively the active recipient has not authenticated or accepted the current disclosure version. | Transfer is denied before commit with no role, membership, ownership, alert, profile, consent, or connection-authority change. The safe outcome is notified as appropriate and audited without exposing protected details. | PM-72-001–005/008; matrix §4.3 permission 29; §6.2 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| CONN-01 | Collaborator completes institution consent for an account they are entitled to access after reviewing resulting budget-space visibility. | Connection is created with the Collaborator as sole authorizer; safe shared financial data becomes readable under role rules; no ownership, membership, or administrative authority is granted; secrets never enter shared views, exports, or audit payloads. | PM-72-002/009/010; matrix §4.3 permission 31 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| CONN-02 | A connection-authorizing Collaborator is removed while synchronization is active. | New synchronization stops immediately; prior imported records/provenance remain; private configuration becomes inaccessible; no owner inherits authority; later rejoining requires reauthorization. | PM-72-003/009; matrix §4.3 permission 31; §6.3 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| CONN-03 | Collaborator authorizer refreshes, repairs, reauthenticates, then disconnects their connection; the Primary Owner, a non-authorizing Co-owner, and another Collaborator attempt the same actions on it. | Authorizer operations succeed under current state; disconnect requires explicit confirmation, stops sync, invalidates provider authorization as supported, and preserves imported history. Every non-authorizer attempt is denied without state change; no authority transfers. | PM-72-002–005/009; matrix §4.3 permission 32 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| CONN-04 | Collaborator authorizer opens connection management and requests both safe operational details and raw credential/token/provider payload fields; non-authorizers request the same interface. | Authorizer receives only approved operational details and controls. Raw secrets/unnecessary payloads are unavailable to everyone. Non-authorizers receive neither private detail nor existence-sensitive diagnostics. No secret enters export or audit payloads. | PM-72-001/002/006/009; matrix §4.3 permission 33 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| CONN-05 | Two members of a joint account independently connect the same external account using their own institution consent. | Two connection records are created, each with exactly one authorizer. Reliable provider identity or explicit confirmation associates them with one logical account; overlapping imports are deduplicated; consent, repair, disconnect, provenance, and audit remain independent. Disconnecting or losing one authorizer stops only that connection while the other may continue sync. | PM-72-009/011; matrix §4.3 permissions 31–33 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| CONN-06 | Primary Owner refreshes, repairs, and disconnects a connection they personally authorized, then attempts the same operations on connections authorized by a Co-owner and by a Collaborator. | Operations on the Primary Owner's own connection succeed after current authorization and connection-state checks; disconnect requires explicit consequence confirmation, stops future sync, and preserves imported records and provenance. Every operation on another person's connection is denied despite Primary authority, changes no connection state, and is audited. Primary ownership confers no connection authority. | Matrix §4.3 permissions 32–33; PM-72-009/011; §6.3 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| CONN-07 | A connection's sole authorizer is permanently gone. The Primary Owner attempts to adopt and reauthorize the connection, then an entitled Collaborator authorizes a new connection to the same logical account. | The orphaned connection never syncs again and is labeled orphaned with its reason. Adoption and reauthorization are denied for every role including the Primary Owner, and its private configuration stays inaccessible to everyone. Imported records and provenance remain readable, so historical totals and reconciliation are unchanged and still explainable. The new connection is created with the Collaborator as sole authorizer, is associated with the same logical account, and overlapping imports deduplicate rather than double-count. No authority transfers at any point. | §6.3; PM-72-009/011; matrix §4.3 permissions 31–33; OD-72-04 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| XSP-01 | Alex is Primary Owner of Household A and a Category-group Viewer in Household B. Acting in B, Alex opens screens, runs a search and a report, and attempts member administration and a connection action. | Household B exposes only the Category-group profile's inherited scope. Owner authority in A confers nothing in B: no member administration, no connection management, no export initiation, and no visibility of B resources outside the profile. Searches, totals, and reports in B are computed only from B-authorized inputs and never include A data. Each attempted administration is denied, changes no state, and is audited in B's budget-space history. A later role change in A alters nothing in B. | PM-72-010/011; §7; matrix §4.3; §5.1 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| XSP-02 | A Household A transaction identifier is submitted while authenticated through a Household B membership, against a read endpoint, a mutation endpoint, and an export request. | Each is denied before any protected detail is returned. The response is indistinguishable from that for a nonexistent resource: no field, count, timestamp, error difference, or observable branch separates not-found from exists-but-forbidden. No state changes anywhere. A cross-space denial audit event is recorded in Household B carrying a safe reason code and no Household A content or identifier echo. | PM-72-010; §5.2; §7; matrix §8 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| XSP-03 | A background report job and a cached report result produced for Household A are replayed and refetched under a Household B membership, and a queued alert delivery for A is retried after the recipient's B membership changes. | Job and cache keys bind budget space, membership, and authorization version, so each fails closed. No result, partial result, count, or existence signal crosses spaces. The queued Household A delivery is neither redirected nor re-evaluated by the Household B change. Each failure is audited in its own space without copying content across, and no correlation identifier leaks either space's data. | PM-72-006/010; §5.2; §5.4.1 items 7–8; matrix §8 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| AUTH-01 | A Collaborator opens a budget-plan edit form and a manual-transaction edit form; before either is submitted an owner changes their role to Viewer. | Both submissions are denied against the current membership version. No financial, planning, interaction, or lifecycle state changes. The interface explains the permission loss and offers a safe recovery path without exposing data the new role cannot read, and entered values are never silently committed. Each denial is audited with actor, acting membership, prior and current role, authorization version, and outcome. | PM-72-002/003/004; matrix §4.1 permissions 2a and 9; matrix §8 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| AUTH-02 | Two Collaborators open the same category target and the same clearly labeled manual transaction, then submit conflicting changes. | Exactly one change commits under its concurrency token. The stale submission is rejected, changes no state, retains the submitter's entered values for retry, and explains the conflict. Confirmed state is never silently overwritten by a last write, recalculated totals and alerts reflect exactly one committed change, and both the commit and the rejection are audited with their concurrency tokens. | PM-72-005; PM-72-007; matrix §4.1; matrix §8 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| AUTH-03 | An authorization request arrives with the Viewer profile absent, then with visibility-group membership absent, then with a stale authorization version. | Each produces denial by default. There is no fallback to role-only authorization, no partial scope, no reuse of a previously cached scope, and no default-open branch. Each denial carries a safe reason code and is audited. No screen, search, report, total, or alert derived from the missing input is offered, and the denial reveals nothing about the scope that would have applied. | PM-72-001; §5.1; matrix §8 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| CAT-01 | An owner or Collaborator attempts to archive a confirmed category that has historical transactions and unresolved future references. | Historical records remain attached and readable. Archival is blocked until future bills, goals, schedules, rules, and other references are reassigned, cancelled, or otherwise resolved. After resolution, archival removes the category from new planning/categorization and prevents new assignments; restoration makes it available again. No hard-delete operation exists. | Matrix §2.3 and §4.1 permission 4; PM-72-002–007 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| REC-01 | Primary Owner, Co-owner, and Collaborator each confirm or reject a suggested income reconciliation and manually match or unmatch expected and actual income. | Authorized operations change only the reconciliation relationship and audited state; expected and actual records remain distinct. Viewer and Accountability Partner attempts are denied without state change. | CBD-68 §13/permission matrix; CBD-71 SD-071-046; matrix §4.1 permission 6 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| REC-02 | Primary Owner, Co-owner, and Collaborator each match, unmatch, split, and dismiss pending/posted reconciliation candidates. | Authorized operations follow CBD-69 §7.3 and retain every bank-provided source record and field. Viewer and Accountability Partner attempts are denied without state change. | CBD-69 §7.3; CBD-71 SD-071-046; matrix §4.1 permission 6 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| DATE-01 | Collaborator overrides a transaction's derived budget date so it belongs to a different budget period. | Period classification, affected totals, and alerts recalculate; retained authorization/posted dates and period boundaries remain unchanged. Audit records actor, before/after dates, reason/provenance, and time. Viewer and Accountability Partner attempts are denied without state change. | CBD-69 EC-69-24; CBD-71 SD-071-050; matrix §4.1 permission 7 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| VIS-01 | Owners, Collaborator, Account-group Viewer, and Accountability Partner open account, balance, and transaction views; the same principal also attempts a cross-space resource ID. | Each role receives only approved visibility and masking. Viewer data is inherited from selected account groups. No response includes credentials, tokens, authentication details, or private configuration; cross-space access is denied without an existence signal. | PM-72-006/010; matrix §4.2 permission 14; §§5/7 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| VIS-02 | Viewer receives Planning profile, then requests accounts, transactions, actual-spending indicators, reconciliation data, and a report requiring actuals. | Plans, category structure, targets, bills, goals, and schedules appear. Every actual-data request is absent or denied; the incompatible report is unavailable rather than silently partial. No hidden data appears as zero or leaks through totals, counts, labels, or shape. | PM-72-001/006; matrix §4.2 permissions 15/19; §§5.1–5.2 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| VIS-03 | Viewer opens history inherited through a Category-group profile; hidden changes involve other categories/resources/actors. The profile later changes before reopen. | Only accessible changes and necessary actor context appear. Hidden changes, actors, relationships, and security-only metadata do not affect counts/shape. Reopen uses the current profile/version and denies or regenerates after scope loss. | PM-72-002/003/006; matrix §4.2 permission 16; §§5.1–5.2 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| VIS-04 | Category-group Viewer can read Housing but not Dining and requests group totals, a whole-budget comparison, chart, trend, and a transaction split across visible and hidden categories. | Supported Housing-only derivations are labeled “Shared view—not the full budget.” Whole-budget output is unavailable. Split activity exposes only the visible allocation as a synthetic category-activity record and reveals no original total, account identity/balance, hidden allocation count, or hidden category. | PM-72-006; matrix §4.2 permission 18; §§5.1–5.2 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| EXP-01 | Collaborator freshly reauthenticates, accepts the irreversible-download warning, and requests the full financial-data export, then requests the customer administrative-history export. | Protected financial export succeeds under generation/download-time authorization with the versioned allowlist, recipient-bound encrypted delivery, short server retention, rate limits, and audit. It excludes every secret, personal-account field, administrative-history field, privileged telemetry item, and cross-space record. Customer administrative-history export is denied without disclosure or state change. | PM-72-002–004/006/007/010; matrix §4.2 permission 20 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| EXP-02 | Primary Owner requests the financial and customer administrative-history packages; Co-owner requests the administrative-history package; Accountability Partner and Viewer request the financial package. | Primary Owner receives both packages subject to their separate allowlists and safeguards. The administrative package contains only safe membership/invitation, role/Viewer-scope, ownership, and protected-action history; redaction occurs before serialization and the recipient-bound package expires within 24 hours. Co-owner administrative-history request is denied. Partner and Viewer bulk financial exports are denied. | PM-72-002–004/006/010; matrix §4.2 permission 20; §5.7 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| EXP-03 | Owner confirms and creates a snapshot for a specifically identified active Viewer from the Viewer's current profile; Collaborator and Viewer attempt initiation; the profile changes before download. | Owner generation contains only §5.8 profile-readable data, excludes every hidden/personal/administrative/security/cross-space class, and creates an encrypted, recipient-bound, non-transferable, non-renewable package expiring within 24 hours. Collaborator and Viewer initiation are denied. Profile/version change invalidates the package and download is denied without a content signal. Every generation, denial, invalidation, download, expiry, and deletion is safely audited. | PM-72-002–004/006/010; matrix §4.2 permission 21; §5.8 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| EXP-04 | Accountability Partner and Viewer each submit direct bulk-export requests while a Collaborator with the same visible financial fact exports under permission 20. | Partner and Viewer requests are categorically denied regardless of breadth of in-app visibility. Collaborator's protected export succeeds because the role is an equal financial contributor; no denial leaks export contents or hidden scope. | PM-72-001–004/006; matrix §4.2 permission 20 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| EXP-05 | Primary Owner requests internal authentication/session evidence, IP/device/network data, connection events, fraud/risk signals, privileged telemetry, support notes, staff identities, or security detection logic in the administrative-history package. | Every excluded field and record class is omitted before serialization. No placeholder, count, shape, reason text, or correlation leaks its existence. The package contains only the §5.7 allowlist, and generation/audit records never copy excluded contents. | PM-72-001/006/010; matrix §4.2 permission 20; §5.7 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| EXP-06 | Administrative-history package is downloaded after the requester loses Primary ownership or after its non-renewable 24-hour lifetime. | Download authorization fails and returns no package or content signal; expiry deletes the server-side package. Source administrative records retain their independent retention lifecycle. Denial and expiry are safely audited. | PM-72-002–005/008; matrix §4.2 permission 20; §5.7 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| REP-01 | Viewer generates a profile-supported report, then an owner changes the profile/scope and the Viewer attempts to reopen a cached result, deliver it externally, and export it. | Initial generation uses complete profile-authorized inputs. Reopen fails or regenerates under the current profile/version; cache cannot cross spaces. External delivery follows personal settings. Bulk export remains denied. | PM-72-002/003/006/010; matrix §4.2 permission 19; §§5.1–5.2/5.4 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| MEM-01 | Co-owner invites a Collaborator, revokes the pending invitation, then removes an active Viewer; Collaborator attempts the same actions and Co-owner attempts to use this permission on the Primary Owner. | Valid non-owner actions succeed under CBD-73 and are notified/audited; active Viewer access and derived artifacts invalidate immediately. Collaborator and ownership-target attempts are denied without state change. | PM-72-002–004/008; matrix §4.3 permission 24; CBD-73 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| MEM-02 | Owner changes a Collaborator to Viewer with no profile, assigns Planning profile, changes Viewer to Partner, then to Collaborator while an old report remains open. | Viewer initially sees nothing, then only Planning scope; leaving Viewer removes the profile/groups; Partner becomes comprehensive and financially read-only with only personal acknowledgements and attributed comments permitted; Collaborator receives approved mutation rights. Old work invalidates and every transition is atomic, notified, and audited. | PM-72-002–005; matrix §4.3 permission 25; §§2.1/5 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| SET-01 | Co-owner changes the budget-space time zone and shared presentation default, then attempts to change another member's notification preference and a bank connection setting; Collaborator attempts the shared-setting changes. | Ordinary shared changes succeed and are audited. Personal-account and connection attempts are denied under their separate permissions. Collaborator attempts are denied without shared-state change. | PM-72-002–004; matrix §4.3 permission 30; §§4.3/5.4 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| ALERT-01 | One informational shared event fans out to eligible recipients; a recipient attempts acknowledgement, then the provisional source resolves. | One deduplicated shared event and one instance per eligible recipient exist; optional delivery attempts are separate. No acknowledgement operation/state exists. Resolution closes the shared event, self-clears all open instances, and suppresses queued unsent attempts without changing financial state. | Matrix §4.1 permission 13; §5.4.1; CBD-69 INV-69-23 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| ALERT-02 | Owner attempts to change another member's trigger threshold, pause their alerts, or dismiss their instance; recipient changes external delivery settings and dismisses their own in-app instance. | Cross-account and trigger changes are denied because MVP thresholds are built in and alert state is personal. Recipient external settings change only their delivery; personal dismissal removes only their instance from the active view and changes no alert fact, eligibility, or other instance. | Specification §5.4; CBD-69 §12.2; FF-009 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| ALERT-03 | One firm shared event creates instances for an owner, profile-eligible Viewer, and Partner; the owner acknowledges and the Viewer archives. | Shared settled fact remains unchanged. Only the owner's instance becomes acknowledged; only the Viewer's instance leaves that recipient's active view; Partner instance remains unchanged. Each state transition is correlated and audited at the instance layer. | Matrix §4.1 permission 12; §5.4.1 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| ALERT-04 | Push delivery fails twice and succeeds on retry for one eligible recipient while another recipient has external delivery disabled. | Both mandatory in-app instances exist exactly once. Three correlated push attempts record failure/failure/success without creating another event or instance. The disabled recipient has no external attempt; their instance remains. Raw destinations are absent from shared/audit views. | §5.4.1; PM-72-002/006 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| MAN-01 | An owner or Collaborator attempts to remove a manual transaction involved in an active reconciliation or other financial dependency. | Removal is blocked with no partial state change and a safe dependency explanation. The actor must first complete the supported unmatch, unlink, or correction workflow; removal never silently breaks the relationship. | Matrix §5.5; PM-72-002–005/007 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| MAN-02 | A removed manual transaction reaches the end of its 30-day recovery window. | The system purges the financial payload and retained child content; the transaction cannot be restored. A minimal tombstone retains only internal identity, manual origin, creator/removal actors and times, budget-space identity, purge time, and audit-chain reference—no financial or interaction content. | Matrix §5.5; PM-72-006–007/009 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| MAN-03 | **Proposed, not yet approved.** Primary Owner, Co-owner, and Collaborator each create and edit a manual account, then archive it after confirmation while it has transaction history, and later restore it; Viewer and Accountability Partner attempt each operation. | Create and edit succeed without confirmation and are audited. Archive and restore each require explicit confirmation naming the account and its consequences, matching CBD-236 `p3` §8.6.1's `confirm` obligation on those two operations only. Archival removes the account and its balance from ordinary views, search, reports, and totals while preserving transaction and audit history; restore reverses that against current authorization and state. Viewer and Accountability Partner attempts are denied without state change. | Matrix §4.1 permission 36 (proposed); §5.5; CBD-236 §8.6.1; `OQ-236-011` | Proposed; not yet Product Owner rule approved. If approved, the Co-owner and Collaborator outcomes require a new CBD-236 policy version (`p4`); `p3` §8.6.1 registers only a Primary Owner cell today |
| INT-01 | Accountability Partner adds, edits, and removes their own comment on a readable supported financial record; Viewer attempts to comment. | Partner actions succeed without changing financial state. Edit shows an edited indicator; removal hides the body and retains a neutral tombstone if reply context requires it. Viewer attempt is denied without state change. Every outcome is audited. | PM-72-002–004; matrix §4.1 permissions 11a–11c and §5.6 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| INT-02 | Primary Owner or Co-owner attempts to edit, remove, hide, or moderate a Collaborator's or Accountability Partner's comment. | Every action is denied with no content or target-state change. Budget-space ownership confers no cross-author editorial authority; the denial is audited. | Matrix §4.1 permission 11d and §5.6; PM-72-002–004 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| INT-03 | A comment author loses membership or target visibility, then follows a stale link or submits an edit/removal request. | The comment remains attributed and visible only to current readers of the target. The former author receives no existence signal or content and cannot edit/remove through the lost membership; stale mutations fail without state change and are audited. | Matrix §5.6; PM-72-001–006 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| AUD-01 | An authorized Collaborator categorizes an imported transaction, edits a category target, and removes a manual transaction. | Each mutation writes one audit record carrying event ID, time, authenticated actor, acting membership and role, budget space, action, target type and safe identifier, before/after or semantic delta, decision, policy/rule version, reason code, and correlation ID. Bank-provided fields appear unchanged in the delta. The three records correlate to their recalculation outcomes and are append-only: no later action rewrites or removes them. | PM-72-007; matrix §9; matrix §4.1 permissions 8, 2a, 9 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| AUD-02 | A Viewer attempts a mutation, a Collaborator attempts budget-space deletion, and a non-authorizer attempts to disconnect a bank connection. | Every denial produces an audit record with the same required fields as a success, plus its denial reason code, and changes no protected state. A denial record never contains the data the actor was refused, and its reason code does not disclose the existence, identity, or value of an out-of-scope resource. Denials are distinguishable from successes without inspecting payloads. | PM-72-004; matrix §9; matrix §4.3 permissions 34 and 32 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| AUD-03 | Audit records are written for a bank-connection authorization, a Collaborator financial export, a customer administrative-history export, and an owner-authorized Viewer snapshot. | No record contains credentials, tokens, authentication material, raw provider payloads, private connection configuration, a recipient's raw notification destination, or package contents. Connection records carry only safe linkage; export and snapshot records carry package identifier, scope and authorization versions, outcome, and safe reason code. Restricted values are absent rather than masked in place, so no record leaks their length, shape, or presence. | PM-72-009; §5.7 item 6; §5.8 item 8; matrix §9 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| AUD-04 | A Viewer, an Accountability Partner, a Collaborator, and a Co-owner each open budget-space history for a resource in their scope, then attempt to read security-only audit metadata. | Each sees only history for resources readable under their current role and, for a Viewer, current profile. Actor identity and role appear only where needed to explain accessible content. Security-only audit metadata, denial evidence, and another member's personal alert or delivery state are excluded for every role. No audit view becomes a side channel for resources the reader cannot otherwise see, including through counts, gaps, or sequence identifiers. | §5.3; matrix §4.2 permission 16; PM-72-006; matrix §9 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| LIFE-05 | An Accountability Partner revokes their own consent, and separately an owner revokes a Viewer's consent, while each has open work, a cached report, a queued alert, and a valid snapshot package. | Access ends immediately for both. Open work is invalidated, cached reports are not re-served, queued alerts are suppressed, and the snapshot package is invalidated with its download denied. Neither revocation affects that person's memberships in other budget spaces or their CoBudget sign-in. Required consent and audit history is retained while the consent state reads revoked, and previously authored comments stay attributed. Each revocation notifies the affected parties safely and is audited. | CBD-12-AC16/AC17; §2.1; §5.6 item 7; §5.8 item 2; PM-72-002–004 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| LIFE-06 | A sole Primary Owner has 90 consecutive days of no authenticated activity. A Collaborator requests archival; the Primary Owner signs in during the objection window. The request is later repeated and runs uncontested. | The first request notifies every active member and the Primary Owner, opens the 14-day objection window, and is cancelled outright by the Primary Owner's authenticated activity, leaving the space live and every role unchanged. The second request completes and archives the space under §6.5. Neither request transfers ownership, changes a role, widens a Viewer profile, reaches a bank connection, or unlocks an export; the space retains its Primary Owner. Requester, notifications, objections, cancellation, and completion are all audited. | §6.3; §6.5; matrix §4.3 permission 35; PM-72-008; OD-72-01 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| LIFE-07 | A Primary Owner archives a budget space, requests deletion, restores it within the 30-day window, then requests deletion again and lets the window close. | Deletion is available only from the archived state; a direct live-state purge is not offered. Each request needs fresh reauthentication and confirmation naming the space, notifies every active member, and opens a 30-day restore window during which the space stays archived and readable at each member's archival-time scope. Restoration returns it to archived-without-pending-deletion with roles and profiles intact. After the second window closes the payload purges irreversibly, leaving only the minimal non-financial tombstone with no amount, balance, merchant, category, account identity, roster, or comment body. Audit-chain linkage survives the purge. | §6.4; §6.5; §5.5; matrix §4.3 permissions 34–35; OD-72-02, OD-72-05 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| LIFE-01 | A revoked Collaborator and an expired-invitation recipient each follow previously valid deep links to a transaction, a report, and a pending export package, while an alert delivery for the revoked member sits queued. | Every protected open is denied under current authorization with no existence signal. The pending package download is denied and the package invalidated. The queued delivery is suppressed rather than sent, and no external channel receives it. Previously authored comments remain attributed to the former member and readable by current readers of their targets, but the former member can no longer read, edit, or remove them. No state changes and each denial is audited. | §2.1; PM-72-002–004; §5.6 item 7; §5.8 item 2 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| LIFE-02 | The sole active Primary Owner attempts to revoke their own participation, deactivate their membership, and delete their account, with no transfer recipient available. | Each attempt is blocked with no membership, ownership, connection, or financial state change, because departure would leave the budget space with no owner. The interface presents the two supported unilateral exits — Primary ownership transfer and budget-space archival — so the sole owner is never left without a way out. The budget space retains exactly one active Primary Owner throughout. The block and the offered alternatives are audited. The same person's memberships in other budget spaces are unaffected. | PM-72-008; §§6.3/6.5; matrix §4.3 permissions 29/35 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| LIFE-03 | Primary Owner attempts to delete a **live** budget space with fresh reauthentication and confirmation; every other role submits a direct deletion request against both the live and the archived space. | Deletion is never available from the live state. §6.4 routes it through archival, so the live-state control is not offered and a direct request is denied without partial deletion or state change. Every non-Primary request is denied in both states. Every attempt is safely explained and audited. `LIFE-07` covers the supported archived-state deletion path. | PM-72-001–005/008; matrix §4.3 permission 34; §§6.1/6.4; OD-72-02 (closed) | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| LIFE-04 | Sole Primary Owner archives the budget space after fresh reauthentication while a Collaborator, a Viewer, and an Accountability Partner hold active memberships and a Collaborator-authorized connection is synchronizing; every other role separately submits a direct archival request. | Archival commits atomically and is audited. Financial records, planning history, reconciliation history, interactions, and audit evidence remain intact and readable; nothing is erased. New mutations, invitations, role changes, comments, synchronization, and alert generation stop for every member, and every active member is notified. The Collaborator authorizer retains their separate right to disconnect and revoke provider authorization, and no connection authority transfers. The budget space retains its Primary Owner. Every non-Primary request is denied without state change. The archived space is retained indefinitely with no countdown; restoration is Primary-only and returns every role and Viewer profile to its archival-time value; members keep read-only access at the scope frozen at archival, and `LIFE-08` covers what they may export within it. | Matrix §4.3 permission 35; §§6.1/6.5; PM-72-008/011; OD-72-05 (closed, as amended by RF-72-61) | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| LIFE-08 | In an archived budget space, a Collaborator requests a full financial export, a Viewer submits a direct export request, an owner creates a snapshot for that Viewer, and an Accountability Partner submits a bulk-export request. The Primary Owner then requests deletion and the same four attempts repeat inside the 30-day restore window. | Export follows read scope. The Collaborator's export succeeds against their frozen archival-time scope and returns nothing beyond it, under the full live control set: versioned allowlist, fresh reauthentication, authorization at generation and download, recipient binding, encryption, rate limits, permanent expiry within 24 hours, and complete audit. The Viewer's direct request and the Partner's bulk request are denied without state change and are audited; the owner-created snapshot succeeds bounded by that Viewer's frozen profile and scope groups, disclosing nothing the Viewer could not already read. No archived attempt widens any allowlist or reaches live role state, and no role or profile changes while archived. Every outcome is identical inside the restore window, so notice that the payload will be purged is never notice without a remedy. | §6.5 items 10–11; §6.4 item 2; §5.7; §5.8; matrix §4.2 permissions 20a/20b/21; PM-72-001–004; OD-72-05 as amended by RF-72-61 | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |

## 4. Coverage work remaining

Every scenario carries a stated rule; none remains a placeholder. **Product Owner decision, August 18, 2026:** the rule-level inventory is accepted as this subtask's authorization-test deliverable, satisfying CBD-72-AC37's six case types per protected feature group. Elaborating each scenario into a concrete deterministic fixture — named actors, dates, amounts, versions, and asserted state deltas — is test-design scope, carried by the CBD-94 verification and review inventory (`VT-94-*`) and the QA phase rather than by this document. The scenarios below remain the binding rule-level acceptance evidence those fixtures must implement.

**CBD-72-AC37 case-type coverage.** AC37 requires an allowed, denied, wrong-budget-space, revoked-consent, missing-membership, and out-of-scope-resource case for every protected feature group. Each case type now has at least one scenario:

| Case type | Representative scenarios |
| --- | --- |
| Allowed | `ROLE-01–03`, `REC-01–02`, `COLL-01–02`, `OWN-01`, `CONN-01`, `EXP-01`, `AUD-01` |
| Denied | `ROLE-04`, `COLL-03`, `INT-02`, `OWN-03`, `EXP-04`, `PART-06`, `AUD-02` |
| Wrong budget space | `XSP-01`, `XSP-02`, `XSP-03`, `VIS-01` |
| Revoked consent | `LIFE-05`, `LIFE-01` |
| Missing membership | `LIFE-01`, `LIFE-02`, `CONN-02`, `INT-03`, `OWN-06` |
| Out-of-scope resource | `VIEW-04`, `VIS-02–04`, `REP-01`, `AUTH-03`, `AUD-04` |

Per-feature-group completeness against that matrix is the remaining check, not the existence of each case type. Before approval, expand the inventory into concrete fixtures and prove:

* every matrix row from permission 1 through 35 has positive and negative coverage where meaningful;
* each Viewer profile and scope-group combination is assigned, replaced, removed, and tested for inheritance, invalid cross-profile combinations, report completeness, and derived leakage;
* each protected ownership action covers expired reauthentication, cancellation, stale state, concurrency, notification failure, and audit failure;
* each required audit event has schema and minimization assertions; and
* every cross-space data path includes synchronous request, search/index, cache, background job, export, and notification cases.

## 5. Revision history

| Version | Date | Author | Change | Approval |
| --- | --- | --- | --- | --- |
| 0.1.15 | September 15, 2026 | Claude (product) | **Proposed, not yet approved.** Added `MAN-03` for permission-model row 36 (manual account, closing `OQ-236-011`): create/edit without confirmation, archive/restore with confirmation, dependency-history preservation, and Viewer/Accountability Partner denial. No approved scenario changed; the approved 78-scenario inventory is unchanged until row 36 and `MAN-03` are approved. | Pending Product Owner approval under `PO-CONTRACT-APPROVALS-001` |
| 0.1.14 | August 18, 2026 | Alexander Wohlford — Product Owner | **Approved.** Final package approval of this exact version; the 78 rule-level scenarios are the binding acceptance evidence that test design implements under `VT-94-*`. | **Approved v0.1.14** |
| 0.1.14 | August 18, 2026 | Claude with Alexander Wohlford as Product Owner | Recorded the Product Owner fixture disposition: the rule-level inventory is accepted as the CBD-72 deliverable and deterministic fixture elaboration is routed to test design under the CBD-94 verification inventory (`VT-94-*`). All 78 draft-status cells now point to that route instead of reading “fixture pending”. No scenario rule changed. | Product Owner approved August 18, 2026 |
| 0.1.13 | August 16, 2026 | Claude with Alexander Wohlford as Product Owner | Corrected `LIFE-03` and `LIFE-04`, which still described OD-72-02 and OD-72-05 as open. RF-72-60 closed both on August 15, 2026 but aligned only `LIFE-06` and `LIFE-07`, leaving these two asserting the opposite of the specification: `LIFE-03` claimed permanent deletion does not execute, and `LIFE-04` claimed restoration and retention behaviour remain blocked. `LIFE-03` now tests that deletion is never offered from the live state because §6.4 routes it through archival, with `LIFE-07` covering the supported path. `LIFE-04` now states the closed archival behaviour — indefinite retention, no countdown, Primary-only restoration to archival-time roles and profiles — and points to `LIFE-08` for export. Both draft statuses drop the "blocked by" wording. No rule changed; the catalog was describing superseded rules. | Editorial correction; no new decision |
| 0.1.12 | August 15, 2026 | Claude with Alexander Wohlford as Product Owner | Added `LIFE-08` for the archived-space export path established by RF-72-61: export follows read scope against the frozen archival-time scope, Viewer self-service and Partner bulk export stay denied, an owner-created snapshot remains the Viewer's only route to a copy, and every outcome holds inside the §6.4 restore window. | Product Owner rule approved; deterministic fixture routed to test design (VT-94-*) |
| 0.1.11 | August 15, 2026 | Claude with Alexander Wohlford as Product Owner | Added `CONN-07` for the permanently orphaned connection, `LIFE-06` for member-initiated archival after prolonged Primary inactivity including the objection-window cancellation, and `LIFE-07` for the archive-to-delete lifecycle with restore and purge. Covers the rules closing OD-72-01, OD-72-02, OD-72-04, and OD-72-05. | Product Owner approved; 77 scenarios |
| 0.1.10 | August 15, 2026 | Claude with Alexander Wohlford as Product Owner | Gave real content to the nine remaining `To detail` scenarios across `PART`, `XSP`, `AUTH`, and `LIFE`, so no scenario is a placeholder. Added the `AUD` family, which had no scenarios despite being a documented family, as `AUD-01` through `AUD-04` covering success, denial, secret minimization, and the audit readability boundary. Added `LIFE-05` for revoked consent, the one CBD-72-AC37 case type with no scenario. Added the AC37 case-type coverage table to §4 and corrected its stale `CBD-72-AC13` citation to `AC37`. | Product Owner approved; 74 scenarios, none placeholder |
| 0.1 | August 14, 2026 | Codex with Alexander Wohlford as Product Owner | Created scenario taxonomy and initial authorization inventory. | Draft; review required |
| 0.1.1 | August 14, 2026 | Codex with Alexander Wohlford as Product Owner | Added recipient-controlled personal notification-preference boundaries and recorded Partner/owner negative cases. | Draft; synchronized review required |
| 0.1.2 | August 15, 2026 | Codex with Alexander Wohlford as Product Owner | Removed Co-owner slot contention from appointment and transfer scenarios; added successful multiple-Co-owner appointment and transfer behavior while retaining stale-state denial. | Product Owner approved; synchronized with matrix v0.1.44 |
| 0.1.3 | August 15, 2026 | Codex with Alexander Wohlford as Product Owner | Added direct Viewer/Accountability-Partner acceptance into Primary ownership and negative coverage for inactive recipients, self-transfer, missing authentication, and stale disclosure acceptance. | Product Owner approved; synchronized with matrix v0.1.45 |
| 0.1.4 | August 15, 2026 | Codex with Alexander Wohlford as Product Owner | Replaced the administrative/security export with a customer administrative-history package and added allowlist, categorical exclusion, pre-serialization redaction, ownership-loss, and 24-hour-expiry scenarios. | Product Owner approved; synchronized with matrix v0.1.46 |
| 0.1.5 | August 15, 2026 | Codex with Alexander Wohlford as Product Owner | Standardized Accountability Partner as financially read-only and added a positive contrast scenario for permitted personal acknowledgement and attributed-comment interactions. | Product Owner approved; synchronized with matrix v0.1.47 |
| 0.1.6 | August 15, 2026 | Codex with Alexander Wohlford as Product Owner | Removed duplicate and contradictory scaffold rows, consolidated redundant cases into stronger approved scenarios, and assigned unique IDs to distinct membership, Viewer-alert, Partner-interaction, and Primary-authorizer cases. | Product Owner approved; permission decisions unchanged |
| 0.1.7 | August 15, 2026 | Codex with Alexander Wohlford as Product Owner | Closed OD-72-03 by expanding EXP-03 with the approved owner-authorized Viewer-snapshot allowlist, denials, recipient binding, invalidation, expiry, and audit assertions. | Product Owner approved; synchronized with matrix v0.1.48 |
| 0.1.8 | August 15, 2026 | Codex with Alexander Wohlford as Product Owner | Synchronized the header with the current approved rule inventory while preserving the explicit remaining fixture-completion work. No scenario behavior changed. | Metadata correction approved; synchronized with matrix v0.1.49 |
| 0.1.9 | August 15, 2026 | Claude with Alexander Wohlford as Product Owner | Gave real content to `ROLE-04`, `VIEW-07`, `PART-06`, and `CONN-06`, which RF-72-53 had renumbered but left empty. Added `LIFE-04` for budget-space archival and rewrote `LIFE-02` for the approved sole-owner exit rule. Completed the family table, which documented ten prefixes while the inventory used twenty-one. Sorted the inventory to match that table. Recorded the empty `AUD` family and the missing revoked-consent coverage as explicit gaps. | Product Owner approved; synchronized with matrix v0.1.50 |
